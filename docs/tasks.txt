$html
1) append support array
2) prependTo
3) transform save his state into htmlTool object

bind
1) unbind
2) bind onchange = bind trigger
3) add bind filter
4) replace bind metadata into hidden object and add bind id

object init
1) split attr option to
attr ~ "on-change"
element -> </>
prefix ~ "app-"
range - list of the valid values

MegaFunction
1) add third argument - filter function
filter(firstData, prevFnresult, prevFnLink)
if filter return undefined then next function not evoke

Async
1) add supporting Promise in wait method
3) progress - checkout ready status. If it is 1 then resolve;

object clone
1) check speed of cloning

html
1) update json convertation